import type { LeaderboardRow } from "@/types/database.generated";

export function Leaderboard({ title, subtitle, rows, currentStudentId }: { title: string; subtitle: string; rows: LeaderboardRow[]; currentStudentId?: string }) {
  return <section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-teal-100 sm:p-6"><div><p className="text-xs font-bold tracking-[0.16em] text-amber-700">BẢNG XẾP HẠNG</p><h2 className="mt-1 text-2xl font-black">{title}</h2><p className="mt-1 text-sm text-slate-500">{subtitle}</p></div><div className="mt-5 space-y-2">{rows.slice(0, 10).map((row) => <div key={row.student_id} className={`grid grid-cols-[44px_1fr_auto] items-center gap-3 rounded-xl px-3 py-3 ${row.student_id === currentStudentId ? "bg-amber-50 ring-1 ring-amber-200" : "bg-slate-50"}`}><span className={`grid size-9 place-items-center rounded-full font-black ${row.rank <= 3 ? "bg-amber-300 text-amber-950" : "bg-white text-slate-600"}`}>{row.rank}</span><div className="min-w-0"><p className="truncate font-bold">{row.student_name}{row.student_id === currentStudentId ? " · Em" : ""}</p><p className="text-xs text-slate-500">{row.completed} bài · {formatDuration(row.total_seconds)}</p></div><b className="text-teal-700">{Number(row.total_score).toFixed(1)}đ</b></div>)}{!rows.length && <p className="rounded-xl border border-dashed border-slate-300 p-6 text-center text-sm text-slate-500">Chưa có kết quả đã chấm để xếp hạng.</p>}</div></section>;
}

function formatDuration(seconds: number) { const hours = Math.floor(seconds / 3600); const minutes = Math.floor((seconds % 3600) / 60); return hours ? `${hours}g ${minutes}p` : `${minutes} phút`; }
