"use client";
import { useState, useTransition } from "react"; import { archiveClassroomAction } from "@/features/admin/server/actions";
export function ArchiveClassroomButton({ id, archived }: { id: string; archived: boolean }) { const [open,setOpen]=useState(false); const [message,setMessage]=useState(""); const [pending,start]=useTransition(); if (archived) return <span className="rounded-full bg-slate-100 px-3 py-1 text-sm">Đã lưu trữ</span>; return <><button className="rounded-lg border border-red-300 px-4 py-2 font-semibold text-red-700" onClick={()=>setOpen(true)}>Lưu trữ lớp</button>{message?<p className="mt-2 text-sm">{message}</p>:null}{open?<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4" onClick={()=>setOpen(false)}><div className="w-full max-w-sm rounded-2xl bg-white p-6" onClick={e=>e.stopPropagation()}><h2 className="text-xl font-bold">Lưu trữ lớp?</h2><p className="mt-2 text-sm text-slate-600">Lớp và dữ liệu vẫn được giữ nguyên.</p><div className="mt-5 flex justify-end gap-2"><button onClick={()=>setOpen(false)}>Huỷ</button><button className="rounded-lg bg-red-600 px-4 py-2 font-semibold text-white" disabled={pending} onClick={()=>start(async()=>{const r=await archiveClassroomAction(id);setMessage(r.success?"Đã lưu trữ lớp.":r.message);setOpen(false);})}>Xác nhận</button></div></div></div>:null}</>; }
