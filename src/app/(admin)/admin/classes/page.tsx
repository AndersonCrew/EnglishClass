import Link from "next/link"; import { listClasses } from "@/features/admin/server/admin-service";
export default async function ClassesPage() { const rows = await listClasses(); return <main className="p-6 lg:p-10"><h1 className="text-3xl font-bold">Lớp học</h1><p className="mt-2 text-slate-500">Giám sát vận hành; Admin không sửa nội dung bài tập.</p><div className="mt-6 overflow-x-auto rounded-2xl bg-white/90 shadow-sm ring-1 ring-teal-100"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-teal-50"><tr><th className="p-3">Lớp</th><th>Giáo viên</th><th>Khối</th><th>Năm học</th><th>Học sinh</th><th>Bài đang giao</th><th></th></tr></thead><tbody>{rows.map((r) => <tr className="border-t" key={r.id}><td className="p-3 font-semibold">{r.name}</td><td>{r.teacherName}</td><td>{r.grade_level}</td><td>{r.academic_year}</td><td>{r.studentCount}</td><td>{r.activeAssignments}</td><td><Link className="font-semibold text-teal-700" href={`/admin/classes/${r.id}`}>Xem</Link></td></tr>)}</tbody></table></div></main>; }
