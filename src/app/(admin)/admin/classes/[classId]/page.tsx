import { notFound } from "next/navigation"; import { ArchiveClassroomButton } from "@/features/admin/components/archive-classroom-button"; import { getAdminClass } from "@/features/admin/server/admin-service";
export default async function AdminClassDetail({params}:{params:Promise<{classId:string}>}){const {classId}=await params;const c=await getAdminClass(classId);if(!c)notFound();return <main className="p-6 lg:p-10"><div className="max-w-2xl rounded-2xl bg-white/90 shadow-sm ring-1 ring-teal-100 p-7"><h1 className="text-3xl font-bold">{c.name}</h1><dl className="mt-6 grid grid-cols-[160px_1fr] gap-3 text-sm"><dt className="text-slate-500">Giáo viên</dt><dd>{c.teacherName}</dd><dt className="text-slate-500">Khối</dt><dd>{c.grade_level}</dd><dt className="text-slate-500">Năm học</dt><dd>{c.academic_year}</dd><dt className="text-slate-500">Học sinh</dt><dd>{c.studentCount}</dd><dt className="text-slate-500">Bài đang giao</dt><dd>{c.activeAssignments}</dd></dl><div className="mt-7"><ArchiveClassroomButton archived={Boolean(c.archived_at)} id={c.id}/></div></div></main>}
