import Link from "next/link";
import { notFound } from "next/navigation";
import { getTeacherAssignments } from "@/features/assignment/server/queries";

export default async function AssignmentsPage({ params }: { params: Promise<{ classId: string }> }) {
  const { classId } = await params; const data = await getTeacherAssignments(classId); if (!data) notFound();
  return <main className="mx-auto max-w-6xl px-5 py-8 sm:px-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><Link href={`/teacher/classes/${classId}`} className="text-sm font-semibold text-teal-700">← {data.classroom.name}</Link><h1 className="mt-2 text-3xl font-bold">Bài tập</h1><p className="mt-2 text-slate-500">Tạo, giao bài và theo dõi kết quả của lớp.</p></div><Link href={`/teacher/classes/${classId}/assignments/new`} className="rounded-xl bg-teal-600 px-5 py-3 font-bold text-white">+ Tạo bài tập</Link></div><div className="mt-7 grid gap-4">{data.assignments.length ? data.assignments.map((assignment) => <article key={assignment.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl bg-white/90 shadow-md shadow-teal-100/60 ring-1 ring-teal-100 p-5 shadow-sm"><div><span className={`rounded-full px-3 py-1 text-xs font-bold ${assignment.status === "PUBLISHED" ? "bg-emerald-100 text-emerald-800" : "bg-slate-100 text-slate-700"}`}>{assignment.status}</span><h2 className="mt-2 text-xl font-bold">{assignment.title}</h2><p className="mt-1 text-sm text-slate-500">{assignment.due_at ? `Hạn: ${new Date(assignment.due_at).toLocaleString("vi-VN")}` : "Không có hạn nộp"}</p></div><Link className="rounded-xl border border-slate-300 px-4 py-2 font-semibold" href={`/teacher/assignments/${assignment.id}/results`}>Xem kết quả</Link></article>) : <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">Lớp chưa có bài tập nào.</div>}</div></main>;
}
